// exports.urlToFeature = (url) => {
//   if (!url) return;
//   return url.split("https://www.openstreetmap.org/")[1].trim();
// };

// exports.separateTypeAndId = (string) => {
//   const [type, id] = string.trim().split("/");
//   return { type, id };
// };
